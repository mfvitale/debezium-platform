# Test configuration — disables token authentication by default
# (AgentTokenFilterTest has its own profile that enables it)
agent.token=
agent.config-base-path=${java.io.tmpdir}/test-configs
agent.data-base-path=${java.io.tmpdir}/test-data
